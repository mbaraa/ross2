import * as React from "react";
import Organizer from "../../../models/Organizer";

interface Props {
    organizer: Organizer;
}

const OrganizerCard = ({organizer}: Props): React.ReactElement => {
    return <div className="border-[1px] border-ross2">noob</div>;
};

export default OrganizerCard;